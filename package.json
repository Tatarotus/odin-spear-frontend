{
  "private": true,
  "name": "front",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "build:css": "tailwind build src/styles/main.css -o src/style.css",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-synacor": "^3.0.4",
    "preact-cli": "^3.0.0",
    "sirv-cli": "^1.0.3",
    "tailwindcss": "^2.0.2"
  },
  "dependencies": {
    "autoprefixer": "^10.1.0",
    "axios": "^0.21.1",
    "postcss": "^8.2.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2"
  }
}
